o 完成离散值处理
    o 保存每种属性是否是连续的
    o 对每种属性进行划分，保存划分标准
        o 在loadTrain上加一个属性划分
    o 在建树过程中按照划分标准进行划分
        o 节点的值用0~N-1的string表示
        o 划分方式：使用中间N-1个点来划分N段，首位点不用（便于处理越过最大最小值的情况）
    o loadTest时的处理
    o 分类时的处理
o 完成missingValue
    o 提取和KNN重复的代码片段
o 去除属性读入时前后空格
o 完成CART决策树
    o 计算Gini系数
    o 完成ROOT_CART